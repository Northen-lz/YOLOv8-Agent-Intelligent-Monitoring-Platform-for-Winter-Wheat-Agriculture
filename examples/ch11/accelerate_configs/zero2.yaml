# DeepSpeed ZeRO-2：优化器状态分片（文档 11.6.4）
# 显存节省中等，通信开销低
compute_environment: LOCAL_MACHINE
distributed_type: DEEPSPEED
num_machines: 1
num_processes: 4
machine_rank: 0
mixed_precision: bf16
deepspeed_config:
  zero_stage: 2
  offload_optimizer_device: cpu
