# YOLOv8-Agent 平台 —— 服务器运行依赖（对齐本机 base 环境实测版本）
# 安装顺序由 server_setup.sh 处理：先装此文件，再单独装 torch CPU 版 + ultralytics
# 关键兼容约束（本机实测）：
#   gradio 5.45.0 + starlette 0.47.3 + mcp 1.29.0 组合可正常 launch（starlette 必须 <0.48）
#   qdrant-client 1.19.x 使用新 API（query_points/retrieve），勿降到 <1.19
gradio==5.45.0
gradio_client==1.13.0
# 数据看板趋势图（gr.LinePlot/plotly）与 detection_log 时间序列（pandas）直接依赖，
# 显式钉住避免 gradio 传递依赖浮动
pandas>=2.0
plotly>=5.18
starlette==0.47.3
mcp==1.29.0
openai==2.53.0
qdrant-client==1.19.0
neo4j>=5.14,<7
numpy>=1.26,<3
scikit-learn>=1.3
scikit-image>=0.22
opencv-python-headless>=4.8
onnxruntime>=1.17
python-docx>=1.0
reportlab>=4.0
Pillow>=10
PyYAML>=6.0
requests>=2.31
python-dotenv>=1.0
PyMuPDF>=1.23
